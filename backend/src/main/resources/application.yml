# ========================================
# 本地开发配置 - MySQL 数据库
# ========================================

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/suziblog?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: "000000"
  jpa:
    hibernate:
      ddl-auto: update    # 自动创建/更新表结构
    show-sql: true        # 开发时显示SQL
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

# 可选：H2控制台（调试用）
# spring.h2.console.enabled=true