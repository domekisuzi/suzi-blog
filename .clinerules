# Cline Rules for suzi-blog 项目

## 更新记录规范
每次代码更新后，必须在 `CHANGELOG_CLINE.md` 文件中添加一行记录，格式如下：

```
- [YYYY-MM-DD HH:mm] [功能模块] 简短描述（修复/新增/优化/重构 XXX）
```

### 示例
```
- [2026-03-24 01:20] 跨域配置 修复前后端跨域访问问题
- [2026-03-24 01:30] 时间线页面 新增点击目标显示绑定任务功能
- [2026-03-24 01:37] 绑定任务 API 修复路径冲突问题
```

## 项目结构
- 后端：Spring Boot (端口 8080)
- 前端：React + TypeScript (端口 3000)
- 数据库：MySQL 

## 开发规范
1. 后端 API 路径统一使用 `/api/模块名`
2. 前端 API 调用统一在 `domains/xxx/api/` 目录下管理
3. 修改 Controller 时同步更新 Service 接口和实现
4. 前端组件使用 MUI 组件库

## 重要 API 路径
- Goals: `/api/goals`
  - `GET /{id}/tasks/list` - 获取目标绑定的任务列表
  - `POST /{id}/tasks` - 添加任务到目标
  - `DELETE /{id}/tasks` - 从目标移除任务